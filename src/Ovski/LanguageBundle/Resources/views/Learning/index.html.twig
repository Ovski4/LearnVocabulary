{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Learning list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{#{{ path('learning_show', { 'id': entity.id }) }}#}">{{ entity }}</a></td>
                <td>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('learning_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
