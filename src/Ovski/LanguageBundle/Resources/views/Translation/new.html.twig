{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Translation creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('translation', { 'slug': slug }) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
